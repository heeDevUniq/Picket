<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heeji.picket.mapper.SeatGradeMapper">

    <select id="list" parameterType="java.util.Map" resultType="java.util.Map">
        /* com.heeji.picket.mapper.SeatGradeMapper.list */
        SELECT  SG.SEAT_GRADE_ID    AS seatGradeId
        ,       SG.SHOW_DATE_ID     AS showDateId
        ,       SG.SHOW_ID          AS showId
        ,       SG.SEAT_COUNT       AS seatCount
        ,       SG.PRICE            AS price
        ,       SG.GRADE_NAME       AS gradeName
        ,       (
                    SELECT  COUNT(*)
                    FROM    SEAT S
                    WHERE   S.SEAT_GRADE_ID = SG.SEAT_GRADE_ID
                    AND     IFNULL(S.SEAT_STATUS, 'available') = 'available'
                )               AS remainCount
        FROM    SEAT_GRADE SG
        WHERE   1 = 1
        AND     SG.SHOW_DATE_ID = #{showDateId}
        ORDER BY SG.SEAT_GRADE_ID ASC
    </select>

    <insert id="insert" parameterType="java.util.Map" useGeneratedKeys="true" keyProperty="seatGradeId">
        /* com.heeji.picket.mapper.SeatGradeMapper.insert */
        INSERT INTO SEAT_GRADE (SHOW_ID, SHOW_DATE_ID, GRADE_NAME, PRICE, SEAT_COUNT)
        VALUES (#{showId}, #{showDateId}, #{gradeName}, #{price}, #{seatCount})
    </insert>

    <delete id="deleteByShowId" parameterType="java.lang.Long">
        /* com.heeji.picket.mapper.SeatGradeMapper.deleteByShowId */
        DELETE FROM SEAT_GRADE
        WHERE   SHOW_ID = #{showId}
    </delete>

    <!-- 수정 화면용, 회차마다 같은 구성이라 이름·가격·좌석수로 묶어 한 벌만 반환 -->
    <select id="listByShowId" parameterType="java.lang.Long" resultType="java.util.Map">
        /* com.heeji.picket.mapper.SeatGradeMapper.listByShowId */
        SELECT  SG.GRADE_NAME   AS gradeName
        ,       SG.PRICE        AS price
        ,       SG.SEAT_COUNT   AS seatCount
        FROM    SEAT_GRADE SG
        WHERE   SG.SHOW_ID = #{showId}
        GROUP BY SG.GRADE_NAME, SG.PRICE, SG.SEAT_COUNT
        ORDER BY MIN(SG.SEAT_GRADE_ID) ASC
    </select>

</mapper>
