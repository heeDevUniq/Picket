<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heeji.picket.repository.UserAlarmRepository">

    <select id="likeYn" resultType="int">
        /* com.heeji.picket.repository.UserAlarmRepository.alarmYn */
        SELECT  COUNT(*)
        FROM    USER_ALARM
        WHERE   1 = 1
        AND     SHOW_ID = #{showId}
        AND     USER_ID = #{userId}
    </select>

    <select id="alarmCnt" resultType="int">
        /* com.heeji.picket.repository.UserAlarmRepository.alarmCnt */
        SELECT  COUNT(*)
        FROM    USER_ALARM
        WHERE   1 = 1
        AND     USER_ID = #{userId}
    </select>

    <select id="list" resultType="HashMap">
        /* com.heeji.picket.repository.UserAlarmRepository.list */
        SELECT  *
        FROM    USER_ALARM
        WHERE   1 = 1
        AND     USER_ID = #{userId}
    </select>

    <insert id="insert">
        /* com.heeji.picket.repository.UserAlarmRepository.insert */
        INSERT INTO USER_ALARM (
            SHOW_ID,
            USER_ID,
            INSERT_DATE
        ) VALUES (
            #{showId},
            #{userId},
            NOW()
        )
    </insert>

    <delete id="delete">
        /* com.heeji.picket.repository.UserAlarmRepository.delete */
        DELETE FROM USER_ALARM
        WHERE       1 = 1        
        AND         SHOW_ID = #{showId}
        AND         LIKE_ID = #{likeId}
    </delete>

</mapper>