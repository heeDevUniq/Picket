<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heeji.picket.repository.ShowReviewsRepository">

    <select id="findAllByShowId" resultType="HashMap">
        SELECT * FROM show_reviews WHERE show_id = #{showId}
    </select>

</mapper>